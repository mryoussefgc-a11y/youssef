{% section 'nav' %}
<section class="section">
  <div class="container" style="max-width:760px;">
    <h1>{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>
{% section 'footer' %}

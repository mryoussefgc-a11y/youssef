{%- comment -%} Generic page (About, Contact, etc.) {%- endcomment -%}
<section class="le-section">
  <div class="le-container le-rte-wrap">
    <h1 class="le-h2">{{ page.title }}</h1>
    <div class="le-rte">{{ page.content }}</div>
  </div>
</section>

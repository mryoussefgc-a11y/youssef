{% section 'nav' %}
<section class="section">
  <div class="container">
    <h1>Collections</h1>
    <ul>
      {% for collection in collections %}
        <li><a href="{{ collection.url }}">{{ collection.title }}</a></li>
      {% endfor %}
    </ul>
  </div>
</section>
{% section 'footer' %}
